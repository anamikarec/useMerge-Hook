function UseMerge(wholedata) {
  // console.log("wholedata", wholedata);
  return (
    <>
      {/* {wholedata.map(function (item) {
        return (
          <>
            <div>{`${item.username} ${item.password} ${item.email}`}</div>
          </>
        );
      })} */}
    </>
  );
}

export default UseMerge;
